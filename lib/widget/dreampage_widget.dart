import 'package:flutter/material.dart';

class DreamPageWidget extends StatelessWidget {
  const DreamPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Dream"),
    );
  }
}
